version: "3.8"

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9004:80"
    restart: unless-stopped
    container_name: portfolio-sj
    environment:
      - NODE_ENV=production
